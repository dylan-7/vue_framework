import { Vue, Component } from 'vue-property-decorator';

/**
 *
 * @file 菜单
 * @export
 * @class AppMenu
 * @extends {Vue}
 */
@Component({
  name: 'AppMenu'
})
export default class AppMenu extends Vue {}
